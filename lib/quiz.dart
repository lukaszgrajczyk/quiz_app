import 'package:flutter/material.dart';

class QuizScreenManage extends StatelessWidget {
  const QuizScreenManage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
